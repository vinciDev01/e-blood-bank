# =============================================================================
# eBloodBank — dépendances Python
# =============================================================================
# Installation :  pip install -r requirements.txt
#
# Ne sont listées que les dépendances DIRECTES du projet. pip installe
# automatiquement leurs sous-dépendances (asgiref, sqlparse, tzdata, etc.).
# Versions épinglées pour garantir un environnement identique sur toutes les
# machines (Linux / Windows / macOS) et éviter les erreurs de version.
# =============================================================================

# --- Framework web ---
Django==6.0.4

# --- Base de données MySQL ---
# Pilote MySQL (extension C). Sous Windows, pip installe un wheel précompilé :
# aucune compilation nécessaire pour les versions courantes de Python.
# En cas de problème, alternative 100 % Python : PyMySQL (voir note en bas).
mysqlclient==2.2.8

# --- Génération des QR codes des poches de sang ---
qrcode==8.2

# --- Génération des ordonnances PDF (demandes de sang) ---
reportlab==5.0.0

# --- Traitement d'images ---
# Requis par qrcode (export PNG) et reportlab (logo dans les PDF).
pillow==12.0.0

# =============================================================================
# Note Windows — si `pip install mysqlclient` échoue (compilation) :
#   basculer sur PyMySQL (pur Python, aucune compilation) :
#       pip install PyMySQL
#   puis, en tête de eBloodBank/__init__.py :
#       import pymysql; pymysql.install_as_MySQLdb()
# =============================================================================
